pkgname=libnearfield
pkgver=1.7.0_rc7
pkgrel=3
pkgdesc="The first free NFC SDK and Programmers API"
arch=('i686' 'x86_64' 'arm')
url='http://www.libnearfield.org/documentation/introduction'
license=('LGPL3')
depends=("libusbx" "pcsclite")
source=("http://libnearfield.googlecode.com/files/libnearfield-${pkgver//_/-}.tar.gz")
md5sums=('c110fc774358ad2c6cbff7cbd762fb45')

build() {
  cd $srcdir/libnearfield-${pkgver//_/-}
  ./configure --prefix /usr
  make
}

package() {
  cd $srcdir/libnearfield-${pkgver//_/-}
  make DESTDIR=$pkgdir install
}
