# Generated by gem2arch (https://github.com/anatol/gem2arch)
# Maintainer: Tyler Bennett <tylerb@trix2voip.com>

_gemname=sourcify
pkgname=ruby1.9-$_gemname
pkgver=0.5.0
pkgrel=1
pkgdesc='Workarounds before ruby-core officially supports Proc#to_source (& friends)'
arch=(any)
url='http://github.com/ngty/sourcify'
license=()
depends=(ruby1.9 ruby1.9-ruby2ruby ruby1.9-sexp_processor ruby1.9-ruby_parser ruby1.9-file-tail)
options=(!emptydirs)
source=(https://rubygems.org/downloads/$_gemname-$pkgver.gem)
noextract=($_gemname-$pkgver.gem)
sha1sums=('76d6f13334daa3ebf6f54f0e4797e0bf96fda084')

package() {
  local _gemdir="$(ruby-1.9 -e'puts Gem.default_dir')"
  gem-1.9 install --ignore-dependencies --no-user-install -i "$pkgdir/$_gemdir" -n "$pkgdir/usr/bin" $_gemname-$pkgver.gem
  rm "$pkgdir/$_gemdir/cache/$_gemname-$pkgver.gem"
  install -D -m644 "$pkgdir/$_gemdir/gems/$_gemname-$pkgver/LICENSE" "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}
